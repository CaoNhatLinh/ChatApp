# Feature inventory

Legend: **Complete and verified**, **Implemented but not fully verified**,
**Partially implemented**, **Not implemented**, **Externally blocked**. A row
cannot be complete without real persistence, permission, failure/recovery and
end-to-end evidence.

| ID | Capability | Current status | Evidence / next proof |
|---|---|---|---|
| ID-IDENTITY-001 | Register/login/me/logout | Implemented but not fully verified | Backend tests green; real Cassandra/CORS cookie journey pending |
| ID-IDENTITY-002 | Rotating refresh cookie | Implemented but not fully verified | `RefreshTokenServiceTest`; browser cookie journey pending |
| ID-IDENTITY-003 | User search/profile update | Implemented but not fully verified | `CanonicalBackendServiceUserTest`; live directory data pending |
| ID-SOCIAL-001 | Friendship requests (send/cancel/accept/reject), accepted friends, block and mutuals | Implemented but not fully verified | Contacts exposes pending-request cancellation through canonical `DELETE /friends/requests/{recipientId}`; `FriendshipServiceTest` verifies projection/inbox cleanup and the browser smoke verifies the wire method, path and restored Invite state; live two-user persistence remains pending |
| ID-ROOM-001 | DM/group/private-channel/community-channel creation and member management | Implemented but not fully verified | The create-room UI sends explicit visibility and join policy; `/communities` provides bilingual search/filter cards plus direct/approval joins; service tests cover canonical hydration, archived exclusion, state mapping, atomic membership and resumable approval; `test:e2e:room-create` and `test:e2e:communities` pass; live Cassandra remains pending |
| ID-ROOM-002 | Room chat policy, member roles, ownership transfer, kick/leave, invite/search, room audit | Implemented but not fully verified | Membership capacity, ownership and role revision are authoritative static state; role create/update/delete is ordered against assignment/transfer; room/member chat policies expose canonical state, protect the owner, require moderation reasons and synchronize the admin projection; member management requests bounded 50-row cursor pages, virtualizes variable-height rows, limits default role disclosure, auto-loads at the rendered end and rejects stale-room responses; the bilingual management panel has production-browser proof; clean Cassandra contention remains pending |
| ID-MESSAGE-001 | Cursor message history/idempotent send | Implemented but not fully verified | shard merge and retry tests; message history and sidebar conversation directory use opaque cursors with bounded pages; sidebar loads the next page at a scroll sentinel and retains an explicit recovery action; live persistence/realtime pending |
| ID-MESSAGE-002 | Edit/delete/reaction/read/pin | Implemented but not fully verified | Delete and edit have destructive/draft-preserving browser proof; the required `MessagePage.interactions` contract hydrates reaction aggregates, current-actor state and latest-read summaries with two bounded projection reads per represented bucket; pin/unpin returns canonical state; `test:e2e:message-interactions` proves initial/reload hydration, exact reaction and pin commands, seen state and VI→EN accessibility; clean Cassandra projection recovery/load remains pending |
| ID-MESSAGE-003 | Poll create/vote/close | Implemented but not fully verified | service path exists; live persistence/realtime pending |
| ID-ROOM-003 | Invites and join requests | Implemented but not fully verified | Direct joins preflight capacity and compensate only their own reserved invite use; public-community approval has one canonical request per room/user, conditional operator claims and retryable interrupted approval; clean Cassandra concurrency remains pending |
| ID-NOTIFY-001 | Notifications and notification settings | Implemented but not fully verified | canonical inbox plus bilingual channel/quiet-hour settings UI, room defaults, personal overrides, evaluator-backed precedence, explicit policy endpoint and guarded inbox pagination; canonical controller scans the latest 12 monthly partitions; live Cassandra pagination/provider delivery remain unverified |
| ID-REALTIME-001 | Presence/heartbeat/typing and offline recovery banner | Implemented but not fully verified | STOMP presence uses per-session subscriptions, explicit conversation/friend authorization, Redis Pub/Sub fan-out, expiring per-device heartbeats, bounded batch snapshots, and viewport-scoped client tracking; a scope-aware 50 ms command batcher deduplicates viewport churn and cancels enter/leave pairs before delivery; the client keeps one active scope per target, transfers scope explicitly and deletes the cached snapshot on transfer/final unwatch; rendered surfaces keep unknown neutral; distributed subscription invalidation on access revocation plus live Redis/STOMP multi-node proof remain pending |
| ID-CALL-001 | Audio/video call signalling + native 1–1 WebRTC session | Implemented but not fully verified | Authenticated `/app/call.*` STOMP controller, explicit target-user routing, native `RTCPeerConnection` offer/answer/ICE flow, incoming accept/decline, mute/camera/hang-up UI; live browser/media and reconnect evidence pending |
| ID-MEDIA-001 | Attachments | Implemented but not fully verified | Cloudinary is configuration-gated; local provider is externally blocked |
| ID-SEARCH-001 | Full-text message search and canonical filters | Implemented but not fully verified | `/search` keeps the primary query and scope visible, places sender/reply/mention/type/date/attachment/pin controls in a collapsed native disclosure with an active-filter count, and re-renders all copy immediately on VI/EN changes; Elasticsearch-gated controller/projector and live authorized index proof remain pending |
| ID-ADMIN-001 | Global admin workspace (whole-app capabilities, user search, app-role grant/revoke, status, bounded session/device revoke, runtime health, all-room directory/policy/archive, monthly audit timeline and CSV export) | Implemented but not fully verified | `/admin` UI + `/api/admin/overview` + `/api/admin/audit` + `/api/admin/audit/export` + `/api/admin/conversations/**` + `/api/admin/users/{id}/app-roles|status|sessions|devices`; stateful browser evidence verifies global room policy/archive/restore, user search/role/status/session/device commands, required reasons, confirmations, CSV export, VI→EN and 390px accessibility/layout; live Cassandra persistence and room-directory backfill remain pending |
| ID-MODERATION-001 | User reports and global moderation | Implemented but not fully verified | Message/profile report forms and user report history are wired to `/api/reports` and `/api/reports/mine`; the stateful admin browser journey verifies exact report-resolution plus sanction impose/revoke contracts through `/api/admin/reports` and `/api/admin/sanctions`; Cassandra persistence/expiry unit evidence exists, while live CAS, language classifiers and appeals remain |
| ID-ADMIN-002 | Admin moderation/analytics/investigation | Partially implemented | report queue/resolution, APP/CONVERSATION sanctions, bounded daily analytics, reasoned message/revision inspection and monthly bounded audit CSV export are implemented; language moderation, appeals and long-range metric dashboards remain planned |
| ID-UX-001 | Canonical page system, responsive redesign and bilingual copy | Complete and verified | All retained public, auth, product, recovery, invite, and operator routes use the shared signal-orange/cool-ink system; removed duplicate routes have no compatibility renderer; static copy coverage, Next build, Playwright route smoke, desktop visual QA, and 390px overflow checks pass |
| ID-IDENTITY-004 | Password reset/email verification/social login/MFA | Not implemented | No provider/workflow in repository |
| ID-NOTIFY-002 | Production push notifications | Externally blocked | Requires FCM/APNS/Web Push credentials and worker |
| ID-CALL-002 | Production audio/video media | Externally blocked | Native 1–1 P2P media is implemented; production NAT traversal still requires an explicitly configured reviewed STUN/TURN provider via `NEXT_PUBLIC_WEBRTC_ICE_SERVERS`; group/SFU calls are not enabled |
