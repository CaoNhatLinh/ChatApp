const colors = {
  // Primary branding
  primary: "#2563eb",         // Blue 600
  primaryDark: "#1d4ed8",     // Blue 700
  accent: "#10b981",          // Emerald 500

  // Backgrounds
  background: "#f8fafc",      // Slate 50 (main app bg - sáng)
  surface: "#ffffff",         // White (chat bubble bg, cards)
  surfaceLight: "#f1f5f9",    // Slate 100 (input, hover, lighter surfaces)

  // Borders & Dividers
  border: "#e2e8f0",          // Slate 200

  // Text
  textPrimary: "#1e293b",     // Slate 800 (main text)
  textSecondary: "#64748b",   // Slate 400 (secondary text)
  textMuted: "#94a3b8",       // Slate 400 (timestamps, placeholders)

  // Status
  danger: "#ef4444",          // Red 500 (error, delete)
  warning: "#facc15",         // Yellow 400 (warnings)
  info: "#3b82f6",            // Blue 500 (info, links)
  success: "#22c55e",         // Green 500 (success, online)

  // Chat bubbles
  bubbleOwn: "#2563eb",       // Blue 600 (your message bubble)
  bubbleOther: "#f1f5f9",     // Slate 100 (other's message bubble)
  bubbleOwnText: "#fff",      // White text for your bubble
  bubbleOtherText: "#1e293b", // Dark text for other's bubble

  // Misc
  overlay: "rgba(30,41,59,0.2)", // For modals, overlays (nhẹ hơn)
};

export default colors;
